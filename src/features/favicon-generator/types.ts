export type LoadedImage = {
  element: HTMLImageElement;
  height: number;
  name: string;
  url: string;
  width: number;
};

export type Offset = {
  x: number;
  y: number;
};

export type BackgroundMode = "transparent" | "color";
